# Releases

The current public candidate is `0.1.0-preview.2`. It is a prerelease: source,
installer, schema, lifecycle contracts, and synthetic clean-room checks are
available; private deployment records, off-site recovery, physical voice, and
external-human acceptance are not part of the public artifact. Release
manifests pin component versions and never pull arbitrary `latest` images.
