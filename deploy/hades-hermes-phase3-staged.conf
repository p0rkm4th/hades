# Phase 3 synthetic acceptance only. This exposes the bounded typed catalog
# as STAGED metadata for household identities; it never promotes or creates
# an n8n workflow. Remove this drop-in before any production policy change.
[Service]
Environment=HADES_EPSILON_PHASE3_HOUSEHOLD_CREATION=staged
