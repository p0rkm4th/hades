# Owner-approved, read-only local finance source. The HADES CSV adapter reads
# metadata and transactions for owner answers; it never writes a ledger.
[Service]
Environment="HADES_FINANCE_CSV_PATH=/var/lib/hades-runtime/finance/bk_download.csv"
