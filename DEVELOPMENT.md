# Development

Run syntax, boundary, schema, reconstruction, and public-safety tests before
publishing. Synthetic fixtures must remain obviously synthetic and must not be
used as live product evidence. Public history is an orphan sanitized baseline,
not a place for private deployment archaeology.
