# HADES hardware

The minimum reference host is 2 CPU cores, 8 GiB RAM, and 40 GiB free disk.
A CPU-only path preserves core functionality but may be slow. A local GPU path
is selected only after driver and container usability checks; HADES does not
replace a working driver. A separate existing model server is supported.
