# HADES Capability Ledger

This is a sanitized project-level record of verified capability. Detailed
owner prompts, URLs, account identifiers, runtime topology, and canonical
state checks remain in the private acceptance record.

Last reviewed: 2026-09-21

## Gamma transition

The Daily-Driver Reliability Beta is frozen as an engineering-accepted
baseline at `0dab370`, with the immutable release tag
`daily-driver-reliability-beta`. Owner direct Beta dogfood remains pending and
non-blocking for Gamma. Gamma begins with shared household resources and uses
the Beta acceptance pack only as affected regression coverage after changes.

Synthetic acceptance identities are available for isolated product testing:
an owner-equivalent test identity and two household test identities. They do
not inherit Scotty's conversations, memory, finance, files, or credentials.
Gamma finance and workload tests must remain synthetic.

| Capability | Status | Component / revision | Sanitized evidence | Limitation |
|---|---|---|---|---|
| Owner conversation | PASS + PERSISTENCE | Open WebUI 0.11.1, Hermes 0.14.0 | Authenticated mobile owner chat returned a response and retained it after reload. | Model latency and capabilities depend on the local model. |
| Durable memory | PASS + PERSISTENCE | Hindsight with Hermes memory provider | Owner UI retain/recall and correction returned the current canonical Hindsight fact in a fresh mobile conversation; reload/restart evidence was preserved; automatic retention suppresses shared/live state and transient operational errors. | Hindsight is contextual memory, not live domain truth. |
| Web search and page evidence | OWNER-ACCEPTED / STATIC PAGE EXTRACTION | SearXNG with Hermes provider plus bounded `public_page_read` adapter | SearXNG remains the discovery path; the owner-facing Hermes route now exposes a public static-page reader for explicit “read this page/article” requests. It enforces public HTTP(S), redirect revalidation, HTML/size limits, no credentials/cookies/forms/downloads, and returns `PAGE` evidence distinct from search snippets. Live VM802 acceptance read `https://example.com/` and returned its title and page fact. | Dynamic/interactive pages, login-required pages, and privileged browser workflows remain separate and unavailable through this read-only path. |
| Adaptive model capability notice | PASS | Open WebUI extension and Hermes deployment overlay | Tool-capable and completion-only local models are presented with distinct capability messaging and routing behavior. | Completion-only models cannot use HADES tools. |
| Distributed inference catalog | DOGFOOD GREEN / OWNER-VISIBLE | Open WebUI native Ollama providers plus private Hermes/OpenAI-compatible routes | Authenticated model discovery now exposes Hermes Agent, Code, Tartarus Hades Fast/Deep, and Hypnos completion-only lanes; live Tartarus, Hypnos, and Hermes route probes return real responses. No laptop endpoint is present. | Continue latency/quality qualification per lane; completion-only models remain toolless and owner policy still governs privileged routes. |
| HADES themes/effects | PASS + PERSISTENCE | Open WebUI extension assets | Authenticated Tailscale mobile DOM verification confirmed native Settings ordering, all HADES selectors, Odysseus Neon plus Rain canvas/classes, and reload persistence. | Effects remain HADES-owned styling layered onto upstream UI. |
| Restart/recovery | PASS | HADES runtime composition | Core health endpoints recovered after controlled service restarts without losing accepted state. | External integrations are not included in this result. |
| Hermes compatibility overlay | PARTIAL | `hermes/sitecustomize.py` | Production 0.14.0 smoke, capability/memory regressions, real private-MCP initialize/list-tools qualification, bounded timing telemetry, and the hardened Hermes 0.21.2 HADES core contract pass. | Full 0.21.2 qualification and owner-authenticated promotion rehearsal remain pending; retain the overlay until upstream parity is proven. |
| Agent Zero runtime | PASS | Agent Zero v2.12, pinned container image | Private loopback deployment is healthy; real Agent Zero UI produced `A0-READY` with local qwen3:8b. | Delegated context persistence and broader operator tasks remain future work. |
| Agent Zero delegation | PASS | Agent Zero + Hermes MCP adapter | Secret-free adapter bounds, disposable synthetic API/MCP success/failure behavior, private runtime boundaries, authenticated production marker task, and pre-dispatch rejection of credential/infrastructure/write-capable task text pass. | Persistent delegated context, broader operator tasks, and interactive operator acceptance remain future work; keep the operator bounded and private. |
| Agent Zero native A2A | DEFERRED FOR V1 | Agent Zero native card + Hermes client | Native card/transport was reviewed against the bounded MCP bridge and is not mutually compatible with the current Hermes client contract. | Revisit only after a compatible authenticated card/transport contract passes bounded task, failure, and household-denial acceptance without increasing authority. |
| Household / Grocy runtime | PASS | Grocy v4.7.1, pinned LinuxServer image | Private loopback deployment is healthy with persistent synthetic database storage. | Gamma is extending this into a shared household contract; Grocy remains canonical. |
| Household / Grocy | GAMMA ACTIVE | Grocy + Hermes | Existing canonical reads, bounded mutations, recipes, receipt review/apply, restart persistence, and synthetic mobile smoke remain accepted. Gamma is now adding explicit household sharing, actor attribution, concurrency, and poor-language acceptance. | Shared-resource attribution, concurrent mutation/duplicate handling, and full multi-user household acceptance are pending. |
| Recipe URL/paste ingestion | OWNER-GATED | `integrations/recipe-ingest/` + Hermes private `mcp_servers` registration | Bounded Schema.org JSON-LD extraction from URLs, conservative visible-text fallback for clearly sectioned pages, pasted HTML/JSON-LD/text convergence, conservative ingredient normalization, exact semantic unit/product resolution, strict value validation, separate preview/apply MCP tools, source/evidence preservation, review warnings, SSRF/size boundaries, portable generated-profile registration, and disposable real-Grocy recipe plus `recipes_pos` read-back pass. | Deploy the private profile and complete one owner-visible preview → confirmation → canonical read-back sequence; site-specific fallback remains intentionally bounded. |
| Receipt / document OCR | DOGFOOD GREEN / OWNER-CONFIRMED APPLY | Official `paddleocr-mcp==0.8.5` + isolated Python 3.11 worker; `integrations/receipt-ocr/` and owner-scoped WebUI upload | Official MCP CLI contract, non-root worker image, persistent disposable model cache, live synthetic merchant/item/total recognition through the VM802 loopback Streamable HTTP gateway, image-only boundary with path rejection, exactly one bounded tool, explicit empty-result failure classification, confidence/review handling, protected bounded fingerprint ledger with `NEW` / `SUBMITTED` / `APPLIED`, strict reviewed quantity/unit apply-plan validation, total mismatch detection, inline item/price parsing, high-confidence fuzzy candidate suggestions that remain review-required, live synthetic OCR → explicit human mapping → Grocy preview → owner-confirmed apply → canonical read-back → compensating cleanup, duplicate no-op reconciliation, and a same-origin WebUI receipt upload that presents a household-friendly review instead of raw JSON. oneDNN/PIR failure is avoided by the explicit CPU fallback and recorded as upstream-sensitive. | Broader household/browser acceptance and more varied real receipt accuracy remain follow-up work; unresolved OCR stays review-required and finance remains a separate owner-gated path. |
| Memory + household composition | PASS | Hindsight + Grocy | A remembered recipe fact was retained, corrected, recalled in a fresh mobile DOM conversation, and combined with live Grocy fulfillment without changing canonical Grocy state; mixed-domain synthetic dogfood, source contradictions, long-context isolation, and transient-error hygiene contracts also pass. | Authenticated HADES multi-user model-quality and owner-UI follow-up evidence remain future work; Grocy remains authoritative for live state. |
| Homelab read-only integration | PARTIAL / PROXMOX + NETBOX + KUMA LIVE | `integrations/homelab-readonly/` over Proxmox / NetBox / Uptime Kuma plus bounded Nmap evidence worker | Read-only MCP is registered in the Hermes profile; protected PVEAuditor, NetBox, and published Kuma status inputs return through the authenticated owner HADES API path; NetBox contains seven observed records; disposable restored NetBox and Kuma reads pass; the expanded synthetic fixture covers IP assignments, inventory-only records, fresh/stale availability, degraded/stopped resources, authority conflicts, and write rejection; bounded Nmap runner/XML normalization, review-only candidate projection, and write-free control planning pass. The current LAN audit identifies `.113` as an excluded Sony PS5 rather than a server. Tartarus and Hypnos SSH recovered, their `eno1` profiles autoconnect on boot, native NVIDIA/CUDA/SMART probes pass, controlled reboot persistence passes, and Podman/NVIDIA CDI is configured; GPU-container execution remains open. Hermes' controlled reboot did not restore ARP/SSH/management reachability and requires console/power recovery. Thanatos is reachable with healthy management services but Secure Boot and `nouveau` gate its NVIDIA lane; its `/dev/sdd` historical SMART anomaly is recorded for hardware follow-up. | Close the Rocky GPU-container gate, recover Hermes, resolve Thanatos firmware/driver policy and storage follow-up, locate the planned seventh host, and finish hardware/network acceptance. |
| Finance platform bakeoff | SELECTED / PRODUCTION GATED | Actual Budget canonical; Firefly fallback; Finlynq/Ledgr watchlist | Four disposable synthetic candidates were started; stable Actual client/server synthetic import, repeat reconciliation, sync/reload, transfer-payee, selected-archive restore, and comparative failure/security evidence passed. | Real finance data, providers, and production promotion remain owner-authorized gates. |
| Finance read-only integration | PASS + PERSISTENCE / PRODUCTION GATED | Actual Budget 26.9.0 + HADES three-tool stdio MCP adapter | Synthetic HADES owner DOM selected the staged model, returned the canonical synthetic balance, Actual independently matched it, and a fresh conversation reload retained the result; unavailable-server behavior returned an explicit failure without fabrication. | Gamma selective sharing is synthetic-only and must remain private-by-default; real accounts, providers, credentials, retention, and webhooks remain owner-gated. |
| Selective finance sharing | GAMMA SYNTHETIC GREEN / PRODUCTION GATED | Thin authorization-only policy plus synthetic Actual-shaped read fixture | Synthetic private-by-default resources, explicit user/group read grants, owner-only grant/revoke, operation-time revocation, account/transaction filtering, and admin/shared-channel privilege-collapse denial pass. The policy stores no balances, transactions, or shadow ledger; the fixture is test-only. | Needs a protected synthetic Actual deployment fixture and product-surface integration before any production promotion; no Scotty finance data is used. |
| Local finance import preview | DOGFOOD GREEN / PRODUCTION OWNER-GATED | `integrations/actual-finance-import/` plus owner-scoped WebUI statement inspection | VM802 owner smoke identifies a CSV, summarizes rows/date range, suggests Debit/Credit mapping, asks for the destination account in human terms, and then produces a confirmation-gated Actual preview. Deterministic duplicate analysis, bounded native handoff classification for QIF, OFX, QFX, and CAMT, and synthetic Alpha/Beta dogfood cover owner-only access, malformed input, duplicate reconciliation, and timeout-after-write recovery. No file upload retention, ledger write, or shadow transaction store. | Production Actual import, real file acceptance, account policy, and finance authorization remain owner-gated. |
| Home Assistant | DEFERRED — NO LIVE INSTANCE | `integrations/home-assistant-readonly/` remains future-ready | Credential-free read-only preflight, reusable two-tool REST MCP adapter, selected-entity allowlist, security-sensitive exclusions, unavailable/stale state shaping, and synthetic REST fixture with rejected writes pass. Missing configuration fails closed without network probes. | No live Home Assistant endpoint, token, entity allowlist, or exposure path exists. Do not advertise or expose HA tools until those owner inputs exist. |
| Self-service workloads | GAMMA HOUSEHOLD DOGFOOD GREEN / PRODUCTION GATED | Bounded policy plus constrained executor boundary; Erebus VM 853 Debian 12 Linux-sandbox template | Synthetic contract passes fixed workload catalog, preview/confirmation, owner/share management, quotas, policy-owned placement, owner-only creation, terminal canonical outcomes, idempotent execution, and fail-closed validation. The Rocky 10.2 VM 850 candidate was rejected because Erebus's i7-5930K cannot boot its x86-64-v2 userspace. Debian VM 853 was cloned to disposable VM 854 through the live broker; SSH, completed cloud-init, static networking, active QEMU guest agent, asynchronous task reconciliation, bounded configuration, start, and canonical running read-back passed, then the clone was purged. Authenticated owner DOM preview → confirmation → tagged VM900 creation → Proxmox running read-back passed. Owner status, explicit restart confirmation, explicit delete confirmation, explicit share, and explicit revoke passed through the real DOM. Household A saw and could request an approved action on the shared workload; Household B could not see it; revocation immediately removed Household A's visibility; owner deletion removed the guest and durable ownership record. The live UI avoids `Pool.Audit`, uses approved node/VMID plus `hades-managed` tagging, and restart has a bounded graceful-reboot plus confirmed stop-start fallback. | Production household identities, broader template rollout, and public exposure remain gated; synthetic household sharing is green. |

| Shared Open WebUI Channels | OWNER-ACCEPTED / PRIVATE | Maintained Open WebUI Channels plus the exact HADES compatibility patch in `webui/Dockerfile` | Authenticated disposable acceptance proves Channels are enabled, shared message persistence/restart works, anonymous access is denied, Beta membership does not grant admin configuration, and live preserved-owner private-channel create/post/read/delete acceptance passes after persisted feature-config reconciliation. | Keep Channels private/owner-scoped; do not create shared Hindsight or transfer owner authority through membership. |
| Agent Zero interactive Operator | DEFERRED | Native Agent Zero UI + future protected private route | Native UI remains separate from bounded MCP delegation; fail-closed trusted-proxy/owner-group policy and no-authority-expansion contracts pass. | Deferred until a private reverse-proxy route and trusted identity source can be selected without broadening authority; reevaluate on that explicit architecture/input change. |
| Local push-to-talk voice | STAGED — NODE LIVE / AUTHENTICATED DOM GREEN / OWNER DOGFOOD PENDING | Hermes Compute `voice.local:8766`, node-local faster-whisper `tiny.en` + Piper `en_US-lessac-medium`, Open WebUI OpenAI-compatible audio transport | Bounded authenticated STT/TTS runtime is persistent under user systemd; it attempts CUDA and currently falls back to node-local CPU/int8 because the host lacks `libcublas.so.12`. Health reports `cuda-pending` until first inference and then the effective device. Open WebUI persisted audio engines are configured to the remote endpoint, authenticated browser microphone capture reaches remote STT, the real Read Aloud DOM control returns remote TTS audio without errors, Read Aloud interruption is usable, the native recording-cancel control is now accessible and works, a 32-input synthetic corpus exercises noisy/paused/clipped operational speech, and a spoken-style Grocy read reaches canonical HADES. A controlled authenticated transcription HTTP 500 now produces human-facing retry/type guidance while leaving the composer usable; the owning HADES theme repair is `ab77586`. | No wake word or always-listening behavior. Voice does not authenticate a speaker or authorize actions. Owner/device dogfood, consequential-action confirmation, speech-recognition ambiguity handling, and long-duration acceptance remain open. |
| Epsilon deterministic automation foundation | ACTIVE ROADMAP / PRIVATE INACTIVE CANARY | `integrations/automation/`, `config/epsilon-workflows/server-health-watch.json`, `deploy/templates/n8n-epsilon.compose.yaml`, and `docs/automation-boundary.md` | Provider throughput evidence is recorded; HADES-owned `hades-automation/v1` inventory/preview contract passes hidden-ID, allowlist, sharing, confirmation, and operation-time revocation checks. The private immutable-digest n8n runner is localhost-only, its Server Health Watch canary is inactive, and the dedicated read-only metadata/execution projection passes live verification without exposing nodes, credentials, or payloads. | No schedule, notification, control scope, or workflow execution is enabled. Manny/Orc must approve the first workflow, actor, notification, retention, and promotion before enablement. |
| Decision Plane / System-One Alpha | ENGINEERING ACCEPTANCE PASS — NO PRODUCTION STEERING | `hades_decision/` with `decision-api/v1`, CURRENT control adapter, sanitized 50-case corpus, optional composite recommendations, candidate manifest, privacy gate, shadow harness, replay scorer, and disabled feature-flagged runtime boundary | Backend-neutral typed recommendations, deterministic authorization intersection, safe CURRENT fallback, hash-only shadow evidence, and zero execution/authority surface pass. Expanded CURRENT measures 45/45 intent, 50/50 capability, 43/43 tool-family, 50/50 clarification, and 7/7 reasoning-tier labels at ~0.071 ms p95. SemIf/Qwen3.5-4B and GLiClass small remain rejected; NanoJev is deferred; Jev remains external-only. | No external backend receives private data; no Decision Plane recommendation can grant authority. Live candidate shadowing and production steering remain disabled pending a materially justified future Beta gate. |
| Browser interaction | PASS / ANONYMOUS READ-ONLY | HADES anonymous `browser-research` proxy over Microsoft @playwright/mcp 0.0.81 | Pinned isolated/headless upstream launch through a local request-filtering proxy, explicit public-host allowlist, HTTPS/credential/private-target checks, filtered navigation/read tool surface, filesystem/storage/evaluation/form/submit rejection, strict profile policy, redirect blocking, and real proxy round-trip through HADES against a disposable fixture. | Privileged profile, draft/submit workflows, and authenticated browsing are deferred; the raw upstream tool surface is not registered. |

Private detailed evidence: the deployment acceptance record maintained outside
this repository. This ledger deliberately contains no owner data or secrets.
