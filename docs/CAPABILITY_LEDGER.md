# HADES Capability Ledger

This is a sanitized project-level record of verified capability. Detailed
owner prompts, URLs, account identifiers, runtime topology, and canonical
state checks remain in the private acceptance record.

Last reviewed: 2026-09-14

| Capability | Status | Component / revision | Sanitized evidence | Limitation |
|---|---|---|---|---|
| Owner conversation | PASS + PERSISTENCE | Open WebUI 0.11.5, Hermes 0.14.0 | Authenticated mobile owner chat returned a response and retained it after reload. | Model latency and capabilities depend on the local model. |
| Durable memory | PASS + PERSISTENCE | Hindsight with Hermes memory provider | Owner UI retain/recall and correction returned the current canonical Hindsight fact in a fresh mobile conversation; reload/restart evidence was preserved; automatic retention suppresses shared/live state and transient operational errors. | Hindsight is contextual memory, not live domain truth. |
| Web search | PARTIAL | SearXNG with Hermes provider | SearXNG canonical queries, an initial owner weather turn, a two-turn synthetic API chain, and a credential-free Hermes CLI turn pass; the repaired profile exposes only `web_search` and refuses unsupported inference from incomplete snippets. | Fresh owner-UI follow-up acceptance is still unavailable; SearXNG remains search-only and does not provide extraction. |
| Adaptive model capability notice | PASS | Open WebUI extension and Hermes deployment overlay | Tool-capable and completion-only local models are presented with distinct capability messaging and routing behavior. | Completion-only models cannot use HADES tools. |
| HADES themes/effects | PASS + PERSISTENCE | Open WebUI extension assets | Authenticated Tailscale mobile DOM verification confirmed native Settings ordering, all HADES selectors, Odysseus Neon plus Rain canvas/classes, and reload persistence. | Effects remain HADES-owned styling layered onto upstream UI. |
| Restart/recovery | PASS | HADES runtime composition | Core health endpoints recovered after controlled service restarts without losing accepted state. | External integrations are not included in this result. |
| Hermes compatibility overlay | PARTIAL | `hermes/sitecustomize.py` | Production 0.14.0 smoke, capability/memory regressions, real private-MCP initialize/list-tools qualification, bounded timing telemetry, and the hardened Hermes 0.21.2 HADES core contract pass. | Full 0.21.2 qualification and owner-authenticated promotion rehearsal remain pending; retain the overlay until upstream parity is proven. |
| Agent Zero runtime | PASS | Agent Zero v2.12, pinned container image | Private loopback deployment is healthy; real Agent Zero UI produced `A0-READY` with local qwen3:8b. | Delegated context persistence and broader operator tasks remain future work. |
| Agent Zero delegation | PASS | Agent Zero + Hermes MCP adapter | Secret-free adapter bounds, disposable synthetic API/MCP success/failure behavior, private runtime boundaries, and an authenticated production marker task pass. | Persistent delegated context and broader operator tasks remain future work; keep the operator bounded and private. |
| Household / Grocy runtime | PASS | Grocy v4.7.1, pinned LinuxServer image | Private loopback deployment is healthy with persistent synthetic database storage. | Broader pantry edge cases remain future hardening. |
| Household / Grocy | PASS | Grocy + Hermes | HADES owner-API add, remove, correction/re-add, quantity handling, intake confirmation, consume, recipe fulfillment available/shortage branches, recipe add-missing merge, restart persistence, synthetic recipe creation plus serving-count verification, and serving-adapter failure/restart/duplicate fuzzing were verified; recipe authoring tools are exposed for owner dogfood. | Recipe-authoring browser acceptance, owner-visible model latency, and additional pantry edge cases remain future hardening; Grocy remains authoritative. |
| Recipe URL ingestion | STAGED | `integrations/recipe-ingest/` + Hermes private `mcp_servers` registration | Bounded Schema.org JSON-LD extraction, conservative ingredient normalization, exact semantic unit/product resolution, strict value validation, explicit preview/apply MCP tools, source/evidence preservation, review warnings, SSRF/size boundaries, portable generated-profile registration, and a pinned disposable real-Grocy recipe plus `recipes_pos` read-back pass. | Representative public structured-data URL acceptance, messy-site fallback, and owner acceptance remain. No recipe is written from an unreviewed URL. |
| Receipt / document OCR | SELECTED / STAGED | Official `paddleocr-mcp==0.8.5` + isolated Python 3.11 worker; `integrations/receipt-ocr/` | Official MCP CLI contract, non-root worker image, cached official-model CPU inference, synthetic merchant/item/total recognition, image-backed stdio and loopback Streamable HTTP gateway handshakes with path rejection, candidate Hermes `mcp test` discovery of exactly one bounded tool, confidence/review handling, caller-supplied fingerprint duplicate warning, strict reviewed quantity/unit apply-plan validation, total mismatch detection, and Grocy intake preview contract pass. oneDNN/PIR failure is avoided by the explicit CPU fallback and recorded as upstream-sensitive. | Production profile is not enabled; canonical fingerprint persistence and real Grocy apply/reconciliation, representative messy receipts, and owner acceptance remain. OCR is evidence only. |
| Memory + household composition | PASS | Hindsight + Grocy | A remembered recipe fact was retained, corrected, recalled in a fresh mobile DOM conversation, and combined with live Grocy fulfillment without changing canonical Grocy state; mixed-domain synthetic dogfood, source contradictions, long-context isolation, and transient-error hygiene contracts also pass. | Authenticated HADES multi-user model-quality and owner-UI follow-up evidence remain future work; Grocy remains authoritative for live state. |
| Homelab read-only integration | PARTIAL | `integrations/homelab-readonly/` over Proxmox / NetBox / Uptime Kuma | Credential-independent preparation, least-privilege templates, reusable read-only MCP summary adapter, synthetic runtime/inventory/availability authority reconciliation, stale-observation labeling, conflict disclosure, discovery scope fixture, acceptance prompts, and failure rules pass without contacting a real endpoint. | Runtime reads remain owner-gated; no real homelab endpoint has been contacted; Nmap discovery worker and owner acceptance remain. |
| Finance platform bakeoff | SELECTED / PRODUCTION GATED | Actual Budget canonical; Firefly fallback; Finlynq/Ledgr watchlist | Four disposable synthetic candidates were started; stable Actual client/server synthetic import, repeat reconciliation, sync/reload, transfer-payee, selected-archive restore, and comparative failure/security evidence passed. | Real finance data, providers, and production promotion remain owner-authorized gates. |
| Finance read-only integration | PASS + PERSISTENCE / PRODUCTION GATED | Actual Budget 26.9.0 + HADES three-tool stdio MCP adapter | Synthetic HADES owner DOM selected the staged model, returned the canonical `$3,395.36` Checking balance, Actual independently matched it, and a fresh conversation reload retained the result; unavailable-server behavior returned an explicit failure without fabrication. | Production enablement, real accounts, provider credentials, retention, and webhooks remain owner-gated. |
| Local finance import preview | STAGED | `integrations/actual-finance-import/import_csv.py` | Deterministic local CSV preview, explicit amount/inflow-outflow mapping, duplicate classification, malformed/ambiguous row rejection, and confirmation-gated write-free Actual `importTransactions` request planning with canonical reconciliation requirements pass synthetic contract tests. | Actual adapter execution, canonical post-import reconciliation, real file acceptance, and owner authorization remain. |
| Home Assistant | READY FOR TOKEN / OWNER GATE | `integrations/home-assistant-readonly/` over official REST/MCP paths | Credential-free read-only preflight, reusable two-tool REST MCP adapter, selected-entity allowlist, security-sensitive exclusions, unavailable/stale state shaping, and owner acceptance contract pass synthetic tests; missing configuration fails closed without network probes. | No live runtime integration is provisioned; URL, token, selected entities, and exposure path require owner authorization. |

| Shared Open WebUI Channels | SELECTED / STAGED | Maintained Open WebUI Channels | Upstream Channels are selected as the shared conversation authority; HADES channel/private-memory/capability boundaries and the disposable acceptance sequence are documented. | Disposable pinned-version migration and Alpha/Beta channel dogfood remain; do not create shared Hindsight or transfer owner authority through membership. |
| Agent Zero interactive Operator | SELECTED / STAGED | Native Agent Zero UI + future protected private route | Native UI is kept separate from bounded MCP delegation; current loopback/private deployment, fail-closed trusted-proxy/owner-group policy contract, and no-authority-expansion contract are documented and tested. | No tracked reverse proxy or supported global launcher is present; route, trusted identity source, WebSockets, native UI, mobile, and restart acceptance remain before dogfood-green. |
| Local push-to-talk voice | SELECTED / STAGED | Wyoming protocol + local whisper.cpp STT + evaluated Piper-compatible TTS | Provider-neutral bounded WAV/transcript and Wyoming event-framing contracts, local voice composition, non-authentication boundary, failure contract, and latency measurements are defined; no second HADES runtime or wake-word dependency is introduced. | Pinned whisper.cpp image exits 132 before HTTP on this host and is classified host/image-sensitive; evaluate a compatible STT lane, then TTS selection, Alpha/Beta/Gamma dogfood, and owner-visible latency. |
| Browser interaction | SELECTED / STAGED | Microsoft @playwright/mcp 0.0.81 | Anonymous-first profile policy, isolated/persistent profile separation, direct-extraction preference, and read/draft/submit side-effect contract are documented with an immutable npm integrity value; disposable local navigation and accessibility-snapshot dogfood passes. | Privileged-profile denial and a confirmed side-effect workflow remain; no production browser profile is registered. |

Private detailed evidence: the deployment acceptance record maintained outside
this repository. This ledger deliberately contains no owner data or secrets.
